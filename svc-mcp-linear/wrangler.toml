# Cloudflare Workers configuration for Linear MCP Server
# Note: Cloudflare deployment is out of scope for initial implementation
# This file prepares the project for future Cloudflare Workers deployment

name = "svc-mcp-linear"
main = "src/server.py"
compatibility_flags = ["python_workers"]
compatibility_date = "2025-01-01"

[observability]
enabled = true

# Uncomment for production deployment
# [vars]
# ENVIRONMENT = "production"
